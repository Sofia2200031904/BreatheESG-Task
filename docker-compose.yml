services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: esg_platform
      POSTGRES_USER: esg
      POSTGRES_PASSWORD: esg
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
